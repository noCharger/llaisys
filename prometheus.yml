global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'llaisys'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:8001'] # Assuming running server on host
    scrape_timeout: 10s
